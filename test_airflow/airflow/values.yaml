executor: CeleryExecutor

airflow:
  users:
    - username: admin
      password: admin
      role: Admin
      email: admin@example.com

webserver:
  service:
    type: ClusterIP

redis:
  enabled: true

postgresql:
  enabled: true